# Normalise condition variable

``` r

library(ecTools)
```

## Introduction

This article explains the use the
[`ec_normalise()`](https://ninanor.github.io/ecTools/reference/ec_normalise.md).
Use this function to *normalise* (also called *rescale*) condition
variables based on defined reference levels (also called reference
values).

*coming soon*
